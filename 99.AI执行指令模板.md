因为现在协议已经冻结了，所以以后**不要再让 AI 去设计协议**，而是让 AI **严格执行协议**。

***

## 我建议以后每次新增世界观，都采用固定的开场方式

例如你要做 **海底世界**。

### 第一步（人工）

&#x20;

你先在项目里创建一个空目录。

例如：

```
3.海底世界/
```

里面什么都不用放。

### 第二步（AI）

&#x20;

新建一个对话后，然后发这一条（记得修改3处对应的名字）：

```text
本次任务：

根据项目中的《0.世界观定制协议》文件夹里面的00~05这6个文件，

初始化工作区：

3.海底世界/

创建新的世界观：《海底世界》。

要求：

所有新增文件，

仅允许创建在：

3.海底世界/

不得修改其它世界观。

不得修改协议。

不得新增协议。

不得优化协议。

这是一次世界观初始化。

不是角色设计。

不是图片生成。

不是视频创作。

请先完整阅读协议。

严格按照协议执行。

如果发现协议不足：

记录下来。

不要修改。

等待确认。

开始前，

先输出：

《实施计划（Phase 1）》。

不要修改任何文件。
```

***

然后 AI 会进入 Phase1。

例如：（记得修改对应的名字）

```
Phase1

准备读取：

0.世界观定制协议/
    00
    01
    02
    03
    04
    05

计划输出：

海底世界世界圣经

海底世界物种圣经

海底世界居民资料卡模板

海底世界Character Sheet规范

执行步骤

......

等待确认
```

你确认以后。

再输入：（记得修改对应的名字）

```text
进入 Phase2。

根据协议，

输出：

《海底世界重构方案》。

本阶段：

不要修改文件。

不要创建文件。

仅分析：

将生成哪些文件。

每个文件负责什么。

文件之间如何引用。

最后输出：

《预计创建文件列表》。

等待确认。
```

***

确认以后。

第三步才是真正开始。（记得修改对应的名字）

```text
进入 Phase3。

允许在新世界观的文件夹中：

创建文件。

允许：

修改当前新的世界观目录。

禁止：

修改其它世界观目录或其中的文件。

禁止：

修改协议。

禁止：

新增协议。

禁止：

扩大系统职责。

完成以后：

输出：

1.

创建文件列表。

2.

每个文件摘要。

3.

如果发现协议冲突，

停止并报告。

不要解释设计过程。

直接实施。
```

***

## 以后所有世界观都统一这一套

以后其实就是：

```
新建世界

↓

Phase1
实施计划

↓

确认

↓

Phase2
重构方案

↓

确认

↓

Phase3
真正生成文件
```

这和我们现在做山海经完全一致。

***

# 我还建议再加一句（以后固定写）

我觉得还有一句非常重要，以后建议固定加进去：

```text
如果协议已经能够解决当前问题，

优先遵守协议。

不要提出新的协议建议。

不要进行架构设计。

除非我主动要求优化协议。
```

这一句作用非常大。

否则很多 AI（包括 Codex、GLM、Claude Code 等）看到项目后，会本能地开始：

> "这里还能优化。"

> "建议新增 XX 协议。"

> "建议拆分目录。"

最后又回到设计协议。

而现在我们的阶段已经变成：

> **协议冻结，只负责产出世界观。**

***

## 我建议把这套流程保存成一个固定模板

甚至可以在 `0.世界观定制协议` 目录里新增一个**唯一的操作模板**，例如：

```
99.AI执行指令模板.md
```

它不属于协议，只属于**操作说明**。

以后无论创建：

- 浮光森林2.0
- 海底世界
- 蒸汽朋克
- 神话中国
- 西游世界
- 山海经重构

都直接把里面的 Phase1 → Phase2 → Phase3 指令复制给 AI 即可。

这样你的整个项目就形成了两层：

- **协议层（冻结）**：定义规则，不轻易修改。
- **执行层（可复用）**：每次新建世界观时按固定流程执行。

我认为这是目前最适合你这个 AI 导演工作台项目的工作方式，后续无论使用 Codex、Trae、GLM，还是 VS Code 插件，都可以复用同一套执行流程。
